"use client";

import { AccountAuthForm } from "./AccountAuthForm";

/** @deprecated Use AccountAuthForm. Kept so older imports keep compiling. */
export function AccountLogin() {
  return <AccountAuthForm />;
}
